# Django
SECRET_KEY=saetjhegfsyfngfghjnsf
DEBUG=True

# PostgreSQL
DB_NAME=economat_db
DB_USER=postgres
DB_PASSWORD=132435
DB_HOST=localhost
DB_PORT=5432

# JWT
ACCESS_TOKEN_LIFETIME=60
REFRESH_TOKEN_LIFETIME=7

# Single-session idle window, in minutes (default 15). A logged-in session that
# is not seen within this window frees itself, and the frontend auto-logs-out
# after the same delay of inactivity.
SESSION_IDLE_MINUTES=15

# Default product/restaurant name (single source of truth). Shown until an admin
# sets a custom name from the settings panel, and the target of "restore to
# default". The frontend fallback lives in src/lib/branding.tsx — keep in sync.
DEFAULT_RESTAURANT_NAME=Cuistock